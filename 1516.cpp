#include<bits/stdc++.h>

using namespace std;

int main(void){
    ios::sync_with_stdio(0);cin.tie(0);
    int a;
    int N,times[501];
    queue<int>q[501];
    cin >> N;
    for(int i=1;i<=N;i++){
        cin >> times[i];
        while(a!=-1,cin >> a){
            cout << a << '\n';
        }
    }
}

